# Dependencies
node_modules/

# Next.js
.next/
out/

# Build output
dist/
build/

# Environment variables
.env
.env.local
.env.*.local

# Local build artifacts (e.g. graph-data.json / vector-index.json built against second-brain)
/local-build/
/.build/

# Local verification screenshots — routinely captured against the private second-brain vault
# (per CLAUDE.md, verification prefers live data), so they can contain real names, org data,
# and page content. Never commit: the repo is slated to go public (TOR-10-VQAEhzb) and
# scrubbing git history after the fact is far costlier than regenerating a screenshot.
/local-*.png
/.playwright-mcp/

# Production build output generated fresh by CI into public/ — never committed
/public/graph-data.json
/public/vector-index.json

# Testing
coverage/

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
*.log
npm-debug.log*

# Next.js generated
*.tsbuildinfo
next-env.d.ts

# Vercel
.vercel

# Agent worktrees (background subagent isolation copies)
.claude/worktrees/
